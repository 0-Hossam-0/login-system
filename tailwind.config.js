/** @type {import('tailwindcss').Config} */
module.exports = {
    content: [
      "./index.html",
      "./src/**/*.{js,ts,jsx,tsx}",
    ],
    darkMode: 'media', // This enables automatic dark mode based on user's system preference
    theme: {
      extend: {},
    },
    plugins: [],
  }